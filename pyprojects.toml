[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]  # errors, flakes, isort, pyupgrade, bugbear

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
